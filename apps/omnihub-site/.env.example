# APEX OmniHub - Required Environment Variables
# Copy to .env.local for local development.
# Set ALL variables in: Vercel -> Project -> Settings -> Environment Variables

# -- Supabase (REQUIRED) ----------------------------------------------------
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# -- Application ------------------------------------------------------------
VITE_APP_ENV=production
VITE_APP_URL=https://apexomnihub.icu

# -- Optional Site Overrides ------------------------------------------------
# Internal authenticated shell route. Keep this a same-origin relative path so the
# OmniLink native shell (Capacitor) deep-links into /omnidash, not an external host.
VITE_DASHBOARD_URL=/omnidash
