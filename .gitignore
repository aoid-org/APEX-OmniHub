# APEX local evidence storage
.apex/

# CDK build output
cdk.out/

# Logs
logs
logs.txt
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/evidence/

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
!terraform.auto.tfvars

# Development server
.dev-server.pid
dev-server.log

# Screenshots and artifacts
artifacts/

# RSI runtime-generated inventory (uploaded as CI artifact, not committed)
inventory/

# Hardhat
cache/
typechain-types/
coverage/
!coverage/coverage-summary.json
coverage.json
.coverage

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
*.egg
.venv/
venv/
ENV/
test-results/

# Test artifacts
concurrency_test_temp/

output.txt

# ── Root-level CI/build artifacts ────────────────────────────────────────────
eslint_report*
eslint_log*
eslint_out*
audit_report.json
lint_output*
tsc_report*
search_results*
test-output.css
playwright-report/

# ── Pre-launch hygiene: known CI/test artifacts ──────────────────────────────
raw_test_output*.txt
tsc_output*.txt
test_output*.txt
test_json*.json
test_forge_full*.py
apps/**/dist/

# ── Stale CI artifacts (auto-generated, should never be committed) ───────────
final_eslint.json
security/npm-audit-latest.json
security/npm-audit-prod.json
ERRORS.md
# Claude Code agent worktrees (local only)
.claude/worktrees/


# Auto-generated security posture reports (local tooling output)
reports/security/

# Worldwide Wildcard generated reports
tests/worldwide-wildcard/reports/

# ─── Runtime & Debug Artifacts ─────────────────────────────────────────────
debug_output/
debug_test.cjs

# ─── Volatile Test Reports (source-controlled evidence goes in docs/) ───────
reports/*.json
reports/*.xml
reports/*.html

# ─── OS & Editor Noise ───────────────────────────────────────────────────────
Thumbs.db
*.swp
*.swo

# ─── CI-generated release artifacts (uploaded as workflow artifacts, not committed) ──
release-evidence.json
shadow-preflight.json

# ─── Package Manager Conflicts ───────────────────────────────────────────────
# Uncomment the line below ONLY after package manager is fully canonicalized:
# package-lock.json
.apex-bible-integration/
.apex-bible-source/
