# PR #1109 Audit Addendum (Branch Safety)

Date: 2026-05-11 (UTC)
Branch intent: `codex/audit-and-cross-reference-prs-for-merge`

## Scope correction
This PR branch is a **docs-only audit branch** and must not carry runtime dependency or lockfile drift.

## Current branch policy
- Allowed in this PR: audit documentation and merge guidance only.
- Not allowed in this PR: `package.json` / `package-lock.json` version churn.

## Cross-reference findings
1. Dependabot PR proposes `trufflesecurity/trufflehog` from `3.93.3` to `3.95.2`, while this repository currently pins TruffleHog workflows to `v3.82.7` SHA.
2. Dependabot PR proposes `peter-evans/create-pull-request` from `7.0.11` to `8.1.0`; verify runner compatibility before merge.

## Deterministic merge recommendation
- Do **not** merge both dependency PRs blindly.
- Regenerate both PRs from current default-branch state and merge only after CI and runner-compat verification.

## 100/100 gate (required)
- 25: baseline versions in PR match current repo pins
- 20: only action pins changed (no unrelated workflow logic drift)
- 20: all required CI checks green
- 15: runner compatibility explicitly verified for create-pull-request v8
- 10: SHA pinning preserved
- 10: secret-scanning execution parity preserved

Merge threshold: **100/100** only.
