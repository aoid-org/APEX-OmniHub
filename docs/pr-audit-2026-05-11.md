# PR Audit: Dependabot workflow action upgrades

Date: 2026-05-11 (UTC)

## PRs reviewed
1. chore(deps): bump `trufflesecurity/trufflehog` from `3.93.3` to `3.95.2`
2. chore(deps): bump `peter-evans/create-pull-request` from `7.0.11` to `8.1.0`

## Current repository state
- `trufflesecurity/trufflehog` currently pinned by commit SHA `6961f2b...` with comment `# v3.82.7` in:
  - `.github/workflows/secret-scanning.yml` (2 call sites)
  - `.github/workflows/production-readiness.yml` (2 call sites)
- `peter-evans/create-pull-request` currently pinned by commit SHA `22a9089...` with comment `# v7.0.11` in:
  - `.github/workflows/compliance.yml` (1 call site)

## Deductive compatibility findings

### A) `trufflehog` 3.93.3 -> 3.95.2
- Not directly applicable as-is: repo is on 3.82.7, not 3.93.3.
- This means the PR diff baseline is stale relative to this repository's declared version path.
- Upgrading could still be safe, but the PR should be regenerated against this branch so the version jump reflects true starting state (`3.82.7 -> 3.95.2`) and can be validated in CI from actual current configuration.

### B) `create-pull-request` 7.0.11 -> 8.1.0
- Potential breaking axis: upstream v8 requires newer self-hosted runner (`Actions Runner v2.327.1+`) for Node 24 support.
- If runners are GitHub-hosted, this is usually satisfied by platform updates; if any jobs use self-hosted runners below that threshold, this can break execution.
- Therefore this upgrade is conditionally mergeable only after explicit runner-version verification.

## Merge decision
- **Do not merge both PRs as-is in a single blind operation.**
- **Required path:** regenerate and retarget each PR on top of current branch state, then run CI matrix.
- Merge recommendation:
  1. TruffleHog PR: **YES, after rebase/regenerate to current pin and CI pass.**
  2. create-pull-request PR: **YES, only if runner baseline check confirms compatibility; otherwise NO until runners are upgraded.**

## Zero-drift merge gate (100-point rubric)
- [25] Version baseline matches current repo pins (no stale diff): REQUIRED
- [20] Workflow syntax unchanged except version pin updates: REQUIRED
- [20] All workflow jobs pass in CI (including compliance job): REQUIRED
- [15] Self-hosted runner version check for v8 prerequisite documented: REQUIRED
- [10] Security posture preserved by SHA pinning (not floating tags): REQUIRED
- [10] Post-merge scan parity confirmed (secret scanning still executes successfully): REQUIRED

Score rule: merge only at **100/100**.
