# Core dependencies (for pip install -r requirements.txt)
# Prefer pyproject.toml, this is for compatibility

# Temporal.io
temporalio>=1.22.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Redis with vector search
redis[hiredis]>=5.0.0,<6.0.0
redis-om>=0.2.0

# LLM interface
instructor>=1.0.0
litellm>=1.0.0

# Database (Primary: Supabase/Postgres)
supabase>=2.3.0
asyncpg>=0.29.0

# TiDB Vector Persistence (Secondary: Hybrid C6 embeddings)
mysql-connector-python>=8.3.0

# Utilities
aiofiles>=23.2.0
python-dotenv>=1.0.0
httpx>=0.26.0
tenacity>=8.2.0
markupsafe>=2.1.0  # XSS-safe HTML escaping (SonarQube S5131)

# Metrics
prometheus-client>=0.20.0

# FastAPI and Rate Limiting
fastapi>=0.109.0
uvicorn>=0.27.0
slowapi>=0.1.9

# Embeddings
sentence-transformers>=2.3.0
numpy>=1.26.0

# Development and coverage dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.4.0
mypy>=1.8.0
ruff>=0.1.0
black>=23.12.0

# Auth
authlib>=1.3.0
# SSL/TLS compatibility
# pyOpenSSL <24.0.0 uses cryptography internals removed in cryptography>=42.0.0
# (lib.GEN_EMAIL). Pin >=24.0.0 to ensure instructor->botocore->urllib3
# import chain resolves correctly on the CI runner's Python environment.
pyopenssl>=24.0.0
