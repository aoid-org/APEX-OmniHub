# Scoped Ruff override for orchestrator CI stability on this branch.
extend = "pyproject.toml"

# Reliability modules are enforced by dedicated reliability tests + coverage gate.
extend-exclude = [
  "reliability/*.py",
]
