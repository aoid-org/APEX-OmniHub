---
version: 1.0.0
date: 2026-07-02
phase: 0 (Forensic Audit — EXECUTION_CONTRACT_2026-07 §B7)
skill: apex-master-debug-claude (PREDICTIVE mode, evidence-only)
baseline_commit: c8faab2
status: awaiting-owner-review (JR)
---

# Orchestrator Forensic Audit — 2026-07

**Mandate (§B7):** audit every file in `activities/`, `core/`, `workflows/`, `security/`,
`infrastructure/`, `omniboard/`, `omnilink/`, `policies/`, `providers/database/` — actual
purpose from code, test coverage, doc-match. Cross-reference `core/intent_registry.py`,
`core/model_registry.py`, `activities/universal_intents.py` against real call sites.
**No fixes were applied.** Every claim below cites a file path, test name, or command output.

## 0. Verified Baseline (measured in this audit, 2026-07-02)

| Gate | Result | Evidence |
|---|---|---|
| `pytest -q` (== `npm run test:py`) | **972 passed, 20 skipped, 14 warnings** in 15.31s | run in clean venv against `orchestrator/pyproject.toml [dev]` |
| `ruff check .` (== `npm run lint:py`) | **All checks passed** | same venv |
| `ruff format --check .` | **108 files already formatted** | same venv |
| Python module count (non-test) | 44 modules across audited dirs + root (`main.py`, `server.py`, `config.py`, `metrics.py`, `sitecustomize.py`, `models/`, `observability/`) | `find . -name "*.py"` |

Deprecation warning observed (not fixed, per §B7): `infrastructure/cache.py:623` uses
deprecated `redis.close()` (redis-py ≥5.0.1 wants `aclose()`).

## 1. Per-File Inventory

Legend — **Cov:** named test file, or "none found". **Doc:** ✓ matches docs, ✗ mismatch (see §2), — not doc-described.

### activities/

| File | Actual purpose (from code) | Cov | Doc |
|---|---|---|---|
| `__init__.py` | Re-exports activity callables for worker registration | n/a | ✓ |
| `compensation_catalog.py` | Registry mapping side-effectful activities → compensation handlers (saga undo) | `test_apex_control_plane.py` | ✓ |
| `dlq_alert.py` | Dead-letter alert: structured ERROR log + Prometheus counter + optional Slack webhook on permanent workflow failure | `test_dlq_alert.py` | ✓ |
| `iron_law_verify.py` | Physical-AI safety gate: bridges TS Iron Law deductive verification into Python before hardware actuation | `test_iron_law_verify.py` | ✓ |
| `man_mode.py` | MAN gate activities: `risk_triage`, `create_man_task`, `resolve_man_task`, `get_man_task`, `check_man_decision` (idempotent, retryable) | `test_man_mode_activities.py`, `test_man_mode_integration.py` | ✓ |
| `notify_man_task.py` | Idempotent operator push-notification for pending MAN approvals | `test_man_mode_integration.py`, `test_core_intents.py` | ✓ |
| `omni_policy.py` | Temporal wrapper (`evaluate_policy`) around `security/omni_policy.evaluate_policy` — tool-execution choke point | `test_omni_policy.py` | ✓ |
| `omnitrace_activities.py` | DB writes for OmniTrace run/event recording | `test_omnitrace_activities.py` | ✓ |
| `physiomni_activities.py` | Physical-edge telemetry: 14-day FFT/RMS baseline, evaluation, alerts, MAN escalation, work orders | `test_physiomni_activities.py` | ✓ |
| `resolve_intent.py` | Replay-safe registry lookup as an Activity (`registry.resolve_or_offline`, `resolve_intent.py:45`) — determinism under hot-deployed intents | `test_resolve_intent.py` | ✓ |
| `tool_registry.py` | Dataclass registry of executable tools (no module docstring) | `test_tool_validation.py`, `test_agent_saga_activity_dispatch.py` | — |
| `tools.py` (1148 ln) | Core external-I/O activities incl. `check_semantic_cache` (gated at `tools.py:149`, see §4), LLM plan generation, DB/email/webhook/YouTube tools | `test_tools.py`, `test_tools_extended.py`, `test_activities_security.py` | ✓ |
| `universal_intents.py` | 3 first-class intent-routable activities: `system.health_check` (:35), `system.echo` (:56), `system.list_intents` (:78) — dual-registered with Temporal and IntentRegistry | `test_universal_intents.py` | ✓ |

### core/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `intent_registry.py` | Universal Intent Registry: dict-based intentId→activity routing, fail-closed `resolve_or_offline` (:140), `register_intent` (:83), `map_intent` (:113) | `test_resolve_intent.py`, `test_universal_saga.py` | ✓ |
| `intents.py` | Registry seed: imports `universal_intents` for decorator side effects, then bridges 14 pre-existing activities via `map_intent` (`intents.py:27-48`) | `test_core_intents.py` | ✓ |
| `model_registry.py` | `ModelProviderRegistry`/`ModelProviderConfig` — BYOM governance (AEGIS PII, VERITAS tool-call safety, RSI) | `test_model_registry.py` | **✗ unwired — see §3.3** |

### workflows/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `agent_saga.py` (1487 ln) | `AgentWorkflow`: event-sourced saga with compensation; calls `check_semantic_cache` at `agent_saga.py:476,659-667` | `test_agent_saga.py`, `test_agent_saga_activity_dispatch.py`, `test_chaos.py` | ✓ |
| `physiomni_saga.py` | `PhysiOmniAnomalySaga`: telemetry envelope compare → Guardian confidence → MAN escalation <90% → work order | `test_physiomni_saga.py` | ✓ |
| `universal_saga.py` | `UniversalOrchestratorWorkflow`: pure execution engine — EventEnvelope → `resolve_intent` activity → dynamic `execute_activity` → OmniModalSchema | `test_universal_saga.py` | ✓ |

Note: `agent_saga.py` (1487 ln), `tools.py` (1148 ln), and `test_tools_extended.py` (1491 ln)
**exceed the 600-line CLAUDE.md ceiling**. Pre-existing; splitting is Phase 1+ work, not audit scope.

### security/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `guardian_fabric.py` | Proprietary Guardian allow/deny policy-decision fabric over `ApexExecutionEnvelope` | `test_apex_control_plane.py` | ✓ |
| `omni_policy.py` | Cached deterministic policy evaluation: DB-fetched policies, TTL cache, exact-string matching (no regex/exec), priority ordering | `test_omni_policy.py` | ✓ |
| `prompt_sanitizer.py` | Prompt-injection defense (CVE remediation for direct interpolation of user goals) | `test_prompt_sanitizer.py` | ✓ |
| `request_signing.py` | HMAC verification of Edge→orchestrator requests; toggle `ORCHESTRATOR_REQUIRE_SIGNATURE` (default true) | `test_request_signing.py` | ✓ |
| `ssrf.py` | URL validation: scheme allowlist + private/loopback/link-local/reserved IP rejection (v4+v6) | `test_ssrf.py` | ✓ |

### infrastructure/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `cache.py` (624 ln) | `SemanticCacheService`: plan-template extraction, sentence-embedding vector search, parameter rehydration (see §4) | `test_cache.py` (657 ln) | ✓ (code) / ✗ (prod claims, §2) |
| `tidb_persistence.py` | Optional TiDB vector persistence; TLS-required; only when `VECTOR_PROVIDER == "tidb"` | `test_tidb_persistence.py` | ✓ |

### omniboard/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `_redis.py` | Fail-closed Redis client factory (typed 503 when `UPSTASH_REDIS_URL` absent) | indirect via `tests/omniboard/*` | — |
| `fsm.py` | Deterministic onboarding FSM | `tests/omniboard/test_fsm.py` | ✓ |
| `router.py` | FastAPI routes for onboarding sessions (Redis-persisted FSM context, 1800s TTL) | `tests/omniboard/test_router.py`, `test_router_contract.py` | ✓ |
| `schema.py` | `ConnectionSpec`, `OmniBoardState` Pydantic contracts | indirect via `tests/omniboard/*` | — |
| `service.py` | OmniBoard service incl. mock-OAuth gated by `OMNIBOARD_MOCK_OAUTH` (non-prod only) | `tests/omniboard/test_service.py` | ✓ |

### omnilink/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `scopes.py` | Permission/constraint matching (`OmniLinkScopes`, wildcard + prefix `match_permission`) | `test_omnilink_scopes.py` | — |

### policies/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `man_policy.py` | Stateless, pure-function MAN risk classification (no I/O); `check_semantic_cache` listed in its low-risk allowlist (`man_policy.py:76`) | `test_man_mode.py` | ✓ |

### providers/database/

| File | Actual purpose | Cov | Doc |
|---|---|---|---|
| `_validation.py` | Single-source table allowlist + identifier validation shared by all providers (SQL-injection guard) | `test_database_provider_contract.py` | ✓ |
| `base.py` | `DatabaseProvider` abstract contract | `test_database_provider_contract.py` | ✓ |
| `factory.py` | Singletons for `DATABASE_PROVIDER` (primary) and `VECTOR_PROVIDER` (TiDB, embeddings only — never primary) | `test_database_provider_contract.py` | ✓ |
| `postgres_provider.py` | asyncpg portable provider (config-only swap: `DATABASE_PROVIDER=postgres` + `DATABASE_URL`) | `test_postgres_provider.py` | ✓ |
| `supabase_provider.py` | Supabase implementation of same contract | `test_supabase_provider.py` | ✓ |

Per A3, this layer was confirmed correct and is **out of remediation scope**.

### Root / support modules (coverage noted for completeness)

| File | Cov |
|---|---|
| `main.py` (worker: registers `AgentWorkflow`, `UniversalOrchestratorWorkflow`, `PhysiOmniAnomalySaga` + full activity set, `main.py:174-200`; also contains a self-test that instantiates `SemanticCacheService` at `main.py:294` — demo path, not worker path) | `test_final_verification.py` |
| `server.py` (FastAPI: `POST /api/v1/goals` → `AgentWorkflow` at `server.py:87-115`; `POST /api/v1/intents` → fail-closed registry check → `UniversalOrchestratorWorkflow` at `server.py:143-198`; `GET /health` :213) | `test_server.py` |
| `config.py` | `test_config.py`, `test_config_security.py` |
| `metrics.py` | `test_idempotency_metrics.py`, `test_server.py` |
| `models/*` (`audit.py`, `events.py`, `execution_envelope.py`, `man_mode.py`) | `test_audit.py`, `test_models.py`, `test_apex_control_plane.py`, `test_man_mode.py` |
| `observability/omnitrace.py` | `test_omnitrace.py`, `test_omnitrace_extended.py` |
| `sitecustomize.py` | **no test found** |

## 2. Doc-vs-Behavior Mismatches (flagged per §B7)

| # | Doc claim | Reality | Severity |
|---|---|---|---|
| M1 | `TEMPORAL_MONITORING.md:15` — health monitor at `scripts/monitor/temporal-health.ts` | **File does not exist**; `scripts/monitor/` absent; no `checkWorkerHealth` anywhere in repo (`grep -rln` scripts/ apps/ supabase/ = 0 hits) | High — doc describes nonexistent tooling |
| M2 | `ARCHITECTURE.md:21` — "Semantic Caching … (70%+ cache hit rate)" | Cache is **disabled in prod** (§4) and hit rate is **not instrumented** — no Prometheus hit/miss counter exists (`grep cache_hit\|hit_rate metrics.py infrastructure/cache.py activities/tools.py` → only a `cache_hit` bool field on the payload model, `cache.py:105,434`) | High — unmeasured number in an architecture doc |
| M3 | `IMPLEMENTATION_SUMMARY.md:9` — "Status: ✅ Production-Ready" (dated 2026-03-16, references stale branch `claude/setup-custom-skills-rJs1h`) | Point-in-time self-declaration; contradicts "CI validates, owner certifies" rule; predates cache prod-disable | Medium |
| M4 | `IMPLEMENTATION_SUMMARY.md:25` + `models/events.py` docstring — "12 APEX apps (AppName enum)" | `AppName` enum has **14 members** (`models/events.py`, incl. `OMNILINK`, `TRADELINE247` beyond the original 12) | Low — count drift |
| M5 | `SEMANTIC_TRANSLATOR.md` (in `orchestrator/`) — 1069-line spec for `SemanticTranslator`/`CanonicalEvent` pipeline | Described code lives **outside orchestrator/** at `src/omniconnect/translation/translator.ts` and is **not production-wired** (§3.4) | High — placement implies orchestrator capability it doesn't contain |
| M6 | `README.md:22` — "Semantic Caching: 70% reduction in LLM calls" | README **itself corrects this** at `README.md:15` (feature-when-enabled, not prod). Honest but the Features list still states the number unqualified | Low |

`README.md` (v1.1.0, audited 2026-06-21) is otherwise the most accurate doc: it already
flags cache-disabled prod state and non-live-verified runtime health (`README.md:7,15`).

## 3. Registry Cross-Reference — genuinely routed vs. referenced-but-unwired (FR5 input)

### 3.1 Intent registry contents (17 total)

- **3 decorator-registered:** `system.health_check`, `system.echo`, `system.list_intents` (`activities/universal_intents.py:35,56,78`).
- **14 bridged via `map_intent`** (`core/intents.py:27-48`): `search_database`, `create_record`, `delete_record`, `send_email`, `call_webhook`, `search_youtube`, `risk_triage`, `create_man_task`, `resolve_man_task`, `get_man_task`, `check_man_decision`, `evaluate_policy`, `verify_deductive_path`, `notify_man_task`.

### 3.2 What is genuinely routed today (end-to-end evidence)

**Legacy goal path — LIVE-wired with real producers:**
frontend/edge callers → `supabase/functions/trigger-workflow/index.ts` (also `apex-agent/index.ts`) → HMAC-signed `POST /api/v1/goals` (`_shared/requestSigning.ts`) → `server.py:87` → `AgentWorkflow`. Frontend call sites exist: `apps/omnihub-site/src/hooks/useOmniModuleState.ts`, `dashboard/components/modules/ModuleShell.tsx`, `LinksModule.tsx`, `DashboardOverview.tsx`.

**Universal intent path — fully wired, no live producer:**
`trigger-workflow/index.ts:398` routes any intent-shaped body (`hasIntentSignal`, :244) → `dispatchIntent` (:263) → `toPythonEventEnvelope` (`_shared/event-ingress-adapter.ts:181`) → signed `POST /api/v1/intents` → `server.py:143` (fail-closed pre-check `if intent_id not in registry`, :168) → `UniversalOrchestratorWorkflow` → `resolve_intent` → registry. Every hop exists in code and is covered by `test_universal_saga.py` / `test_server.py`. **However: zero production frontend call sites send intent-shaped payloads** — `grep -rn 'kind: "intent"|intentId:' apps/omnihub-site` returns 0 hits. The pipe is built and tested; nothing pumps through it yet. This is the FR6 wiring gap, now confirmed and precisely located.

### 3.3 `core/model_registry.py` — referenced-but-UNWIRED (confirmed)

`grep -rn "from core.model_registry|ModelProviderRegistry|AEGIS|VERITAS" --include="*.py"`
across the repo finds **only** `tests/test_model_registry.py`. No import from `main.py`,
`server.py`, any workflow, or any activity. The AEGIS/VERITAS/RSI BYOM governance layer is
dead code with passing tests — it enforces nothing at runtime.

### 3.4 "Universal translation" claim — scaffolding confirmed, production wiring absent

- Implementation: `src/omniconnect/translation/translator.ts` (+ `types/canonical.ts`, `policy/policy-engine.ts`, connectors).
- Tests exist: `tests/omniconnect/semantic-translation.test.ts` et al.
- Production wiring: **none.** Only importer outside `src/omniconnect/` is `src/components/ConnectorKit.tsx` (root demo tree — per CLAUDE.md the production entry is served from `apps/omnihub-site`). The dashboard `TranslationModule.tsx:4` is explicitly a stub "until the @omniconnect/translation package is available in the workspace". `supabase/functions/apex-agent/index.ts:17` mentions omniconnect only for an OAuth-exchange comment.

## 4. Semantic Cache Status (FR4 input) — measured, not assumed

| Fact | Evidence |
|---|---|
| Code default: **enabled** | `activities/tools.py:149` — disabled only when `SEMANTIC_CACHE_ENABLED` ∈ {false,0,no} |
| Prod state: **disabled** | `SEMANTIC_CACHE_ENABLED=false` on the 512 MB Render Starter worker — `docs/APEX_AGENT_OPERATIONS.md:66,92`, `README.md:15`, `memory/omni-recall/docs/CURRENT_PLATFORM_STATE_2026_06_21.md:97` |
| Why disabled | Embedding model (PyTorch) OOMs 512 MB; needs ≥2 GB — `docs/APEX_AGENT_OPERATIONS.md:92,184`; gating commit `be04b92` |
| Miss behavior: **safe** | `check_semantic_cache` returns a clean miss; correctness unaffected — `activities/tools.py:148`, `docs/operations/APEX_AGENT_RUNBOOK.md:149` |
| Hit rate: **not instrumented** | No Prometheus hit/miss counter in `metrics.py` / `cache.py` / `tools.py`; only `PlanTemplate.cache_hit` bool (`infrastructure/cache.py:105,434`). "70%" figures (§2 M2/M6) have no measurement source in this repo |

**FR4 consequence:** "measured hit-rate" requires (a) adding hit/miss metrics (in-scope,
zero-dependency) and (b) either a ≥2 GB worker (**BLOCKED-COST** — JR decision) or a
documented decision that it stays off. Memory profile before any flag flip, per A7.

## 5. A.R.I.S.E. Phase 0 Gaps (FR2/FR3 input)

| Gap | Status | Evidence |
|---|---|---|
| FR3 — CI persists snapshot artifacts | **ALREADY CLOSED** | `.github/workflows/arise.yml:46-48` (upload `arise-structural-baseline`, 90-day retention), `:82-84` (diagnosis report), `publish-snapshot` job `:91+` commits dated snapshots via automation PR. Commits: `78f5215` (#1543), `c8faab2` (#1551 protected-branch guard). Doc: `docs/APEX_AGENT_OPERATIONS.md` §9.25 |
| FR2 — devDependency sign-off since PR #1540 | **OPEN** | PR `f91016b` (#1540) added devDependencies to `apps/apex-arise/package.json`: `dependency-cruiser@^18`, `jscpd@^5.0.11`, `madge@^8`, `ts-morph@^28`, `vitest@^4.1.9`, `@vitest/coverage-v8@4.1.9`, `@types/node@^26`, `typescript@^6.0.3`. **No authorization/sign-off record found**: `grep -rni "authoriz|sign-off|approved" apps/apex-arise/README.md` = 0 hits; nothing in `docs/release/` or `docs/audits/` references these packages. Closing FR2 = writing the sign-off record (docs-only, 1 file) |

## 6. Coverage Gaps (explicit, per A7 risk mitigation)

1. `sitecustomize.py` — no test found.
2. `activities/tool_registry.py`, `omniboard/_redis.py`, `omniboard/schema.py`, `omnilink/__init__.py` — no dedicated test file; covered only indirectly.
3. Cache hit/miss rate — no metrics instrumentation (§4), so `test_cache.py`'s 657 lines prove logic, not production efficacy.
4. 20 skipped tests in the suite — skip reasons not audited individually (would exceed audit scope; list via `pytest -q -rs`).
5. No integration test exercises the **edge→server** HMAC hop (`trigger-workflow` → `/api/v1/intents`); Python side (`test_request_signing.py`, `test_server.py`) and TS side are tested separately.

## 7. UNCERTAIN items (§B4 3-attempt cap applied)

- **UNCERTAIN: live Render/Temporal runtime health.** Not live-verified in this audit (no live-infra access under `apex-master-debug-claude`; §B2.5 assigns live checks to `omnidev-apex-pro-v2`). Docs self-flag the same gap (`README.md:7`). Any live verification belongs to a Phase 1 task under the correct skill.
- **UNCERTAIN: production hit-rate history.** No telemetry source found in-repo; if Render/Grafana dashboards exist externally, they are not referenced by any doc audited here.

## 8. Phase 0 Verdict

Core mechanisms are real, tested (972 passing), and lint-clean. The externally load-bearing
claims decompose as: **saga/goal orchestration — genuinely live**; **universal intent
routing — built and tested end-to-end but with zero live producers (FR6 target)**;
**semantic caching — real code, prod-disabled, efficacy unmeasured (FR4 target)**;
**BYOM governance (`model_registry`) — dead code (wiring decision needed)**;
**universal translation — scaffolding outside orchestrator/, not production-wired**.
Two docs (M1, M5) describe capabilities that do not exist where claimed.

## 9. Streamlining / Decomplexification Opportunities (owner pre-approved 2026-07-02; each is one §B3 task, deferred to Phase 1 — §B7 forbids fixes during audit)

| # | Opportunity | Risk | Files |
|---|---|---|---|
| S1 | Delete or rewrite `TEMPORAL_MONITORING.md` — it specs a script that doesn't exist (M1). Replace with a pointer to the real runbook (`docs/operations/APEX_AGENT_RUNBOOK.md`) | None (docs-only) | 1 |
| S2 | Relocate `SEMANTIC_TRANSLATOR.md` out of `orchestrator/` to sit beside `src/omniconnect/` (M5), with a stub pointer left behind | None (docs-only) | 2 |
| S3 | Fix `IMPLEMENTATION_SUMMARY.md` stale self-certification + "12 apps" count (M3/M4) | None (docs-only) | 1 |
| S4 | `redis.close()` → `aclose()` at `infrastructure/cache.py:623` (kills a deprecation warning; matches redis-py ≥5) | Low, test-covered by `test_cache.py` | 1 |
| S5 | Decide `core/model_registry.py`: wire it at a real choke point or move to an `experimental/` quarantine — dead governance code that *looks* enforced is an audit liability (§3.3) | Decision needed from JR first | TBD |
| S6 | Split `workflows/agent_saga.py` (1487 ln) and `activities/tools.py` (1148 ln) per the 600-line law — single-responsibility extraction, no behavior change, suite must stay 972-green | Medium (needs care; one file per task) | 2 tasks |

**Gate:** this file exists; every claim is path-cited. Awaiting JR review before Phase 1.
