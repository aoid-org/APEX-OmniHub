name: RSI Governance Gate

on:
  pull_request:
    branches: [main]

jobs:
  rsi-governance-gate:
    name: RSI Governance Gate
    runs-on: ubuntu-latest
    steps:
      - name: Pass
        run: echo "RSI Governance Gate — component not yet active, passing."
